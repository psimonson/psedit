TODO List for EDIT by Philip R. Simonson
===========================================================================

 - Not done yet.
 + Almost finished.
 * Completed (Done).

 ==========================================================================

 - Rest of a simple text editor.
 + Handle special keys (e.g. Return, Arrow keys, Page Up/Down, etc.).
 + Simple find feature (Almost done).
 + Fix bug with 'Return' key.
 * Allow you to move the cursor around the screen.
 * Fix bug with scrolling up/down.
 * Fix bug with backspace.
 * Fix more bugs with movement keys, now all current bugs are gone.
